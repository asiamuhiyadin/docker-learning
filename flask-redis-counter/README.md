# DevOps Mission Control

> A production-flavoured Flask + Redis + Docker dashboard — built by Asia.

A containerised web application demonstrating stateless horizontal scaling, Redis persistence, and Nginx load balancing — presented as a polished "Mission Control" dashboard rather than a tutorial project.

---

## Project Structure